language: python
python:
  - "3.6.6"
install:
  - pip install -r requirements.txt
# command to run tests
before_script:
 - cd sendApi
script: 
  - nosetests --with-coverage --cover-tests
  - coverage report

after_success:
  - coveralls2